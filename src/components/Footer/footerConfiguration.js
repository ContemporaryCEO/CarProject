import config from '../../config/default.json'

export const localizedLayout = config.switchEng ? config.layouts.EN_layout.Footer : config.layouts.RU_layout.Footer
export const subfoterLocalizedLayout = config.switchEng ? config.layouts.EN_layout.SubFooter : config.layouts.RU_layout.SubFooter
